spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/smart_expense_tracker_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
application:
  security:
    jwt:
      secret-key: 4hiJCj1Sl2OH/8V0U9rArhTvQBNDx+hAPGGI+LReFJY=
      expiration: 900000 # 15 хвилин у мілісекундах (Access Token)
      refresh-token:
        expiration: 604800000 # 7 днів (Refresh Token)